# SQLite database file (shadow build — local only)
DATABASE_URL="file:./dev.db"

# Mock auth session secret (shadow build only — not real security)
SESSION_SECRET="shadow-5r-dev-secret-ganti-bila-perlu-0a1b2c3d4e5f"
